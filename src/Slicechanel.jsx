import { createSlice } from "@reduxjs/toolkit";

export const Slic = createSlice({
    name: "counter",
    initialState: {chanelid:null,
    chanelname:null},
    reducers: {}
      
})
export const {  } = Slic.actions
export default Slic.reducer